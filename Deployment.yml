apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels: test
      app: nginx
  template:
    metadata:
      labels: test
        app: nginx
    spec:
      containers:
      - name: docker-node
        image: docker-node-app:latest
        ports:
        - containerPort: 80